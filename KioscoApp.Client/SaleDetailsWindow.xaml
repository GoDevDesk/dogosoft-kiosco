<Window x:Class="KioscoApp.Client.SaleDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Detalle de Venta" Height="450" Width="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <!-- grilla -->
            <RowDefinition Height="Auto"/>
            <!-- total -->
        </Grid.RowDefinitions>

        <DataGrid x:Name="GridItems" AutoGenerateColumns="False" IsReadOnly="True" Grid.Row="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Producto" Binding="{Binding Product.Name}" Width="*"/>
                <DataGridTextColumn Header="Cantidad" Binding="{Binding Quantity}" Width="80"/>
                <DataGridTextColumn Header="Precio Unitario" Binding="{Binding UnitPrice, StringFormat=C}" Width="100"/>
                <DataGridTextColumn Header="Subtotal" Binding="{Binding Subtotal, StringFormat=C}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Right" Margin="0,10,0,0">
            <TextBlock Text="Total: " FontWeight="Bold" FontSize="14" VerticalAlignment="Center"/>
            <TextBlock x:Name="TxtTotal" FontWeight="Bold" FontSize="14" VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>
