<Window x:Class="FastFoodApp.Client.AdjustmentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ajuste" Height="371" Width="411"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        PreviewKeyDown="Window_PreviewKeyDown">
    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBlue" Color="#FF3498DB"/>
        <SolidColorBrush x:Key="DarkBlue" Color="#FF2C3E50"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Grid.Row="0" 
                   x:Name="TxtTitulo"
                   Text="APLICAR DESCUENTO"
                   FontSize="16"
                   FontWeight="Bold"
                   Foreground="{StaticResource DarkBlue}"
                   Margin="0,0,0,20"/>

        <!-- Método de cálculo -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <RadioButton Grid.Column="0"
                         x:Name="RbPorcentaje"
                         Content="% Porcentaje"
                         GroupName="Metodo"
                         IsChecked="True"
                         FontSize="13"
                         FontWeight="Bold"
                         VerticalAlignment="Center"
                         Checked="RbMetodo_Checked"/>

            <RadioButton Grid.Column="1"
                         x:Name="RbMonto"
                         Content="$ Monto fijo"
                         GroupName="Metodo"
                         FontSize="13"
                         FontWeight="Bold"
                         VerticalAlignment="Center"
                         Checked="RbMetodo_Checked"/>
        </Grid>

        <!-- Entrada de valor -->
        <Border Grid.Row="2"
                Background="#F8F9FA"
                BorderBrush="{StaticResource PrimaryBlue}"
                BorderThickness="2"
                CornerRadius="5"
                Padding="15"
                Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           x:Name="TxtEtiqueta"
                           Text="Ingrese el porcentaje:"
                           FontWeight="Bold"
                           Margin="0,0,0,10"/>

                <TextBox Grid.Row="1"
                         x:Name="TxtValor"
                         Height="40"
                         FontSize="18"
                         VerticalContentAlignment="Center"
                         HorizontalContentAlignment="Center"
                         BorderBrush="{StaticResource PrimaryBlue}"
                         BorderThickness="2"
                         Margin="0,0,0,10"
                         PreviewTextInput="TxtValor_PreviewTextInput"/>

                <TextBlock Grid.Row="2"
                           x:Name="TxtCalculado"
                           Text="Monto calculado: $0.00"
                           FontSize="14"
                           FontWeight="Bold"
                           Foreground="{StaticResource PrimaryBlue}"
                           HorizontalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Info -->
        <TextBlock Grid.Row="3"
                   x:Name="TxtInfo"
                   Text="Subtotal actual: $0.00"
                   FontSize="12"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Foreground="#666"/>

        <!-- Botones -->
        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button Content="✓ Aplicar (F9)"
                    Width="130"
                    Height="40"
                    Margin="0,0,10,0"
                    Background="#27AE60"
                    Foreground="White"
                    FontWeight="Bold"
                    Click="BtnAplicar_Click"
                    ToolTip="Presione F9 para aplicar"/>

            <Button Content="Cancelar (Esc)"
                    Width="130"
                    Height="40"
                    Click="BtnCancelar_Click"
                    Background="#E0E0E0"
                    Foreground="#333333"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>