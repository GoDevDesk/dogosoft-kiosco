<Window x:Class="FastFoodApp.Client.StockMovementsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Movimientos de Stock" Height="700" Width="1100" 
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBlue" Color="#FF3498DB"/>
        <SolidColorBrush x:Key="DarkBlue" Color="#FF2C3E50"/>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <Border Grid.Row="0" 
                Background="{StaticResource DarkBlue}"
                Padding="10,8"
                Margin="-15,-15,-15,0">
            <TextBlock Text="MOVIMIENTOS DE STOCK" 
                       Foreground="White"
                       FontWeight="Bold"
                       FontSize="16"/>
        </Border>

        <!-- Filtros -->
        <Grid Grid.Row="1" Margin="0,15,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" 
                       Text="Producto:" 
                       VerticalAlignment="Center"
                       Margin="0,0,10,0"
                       FontWeight="Bold"/>

            <TextBox Grid.Column="1" 
                     x:Name="TxtBuscarProducto"
                     Height="30"
                     VerticalContentAlignment="Center"
                     Padding="5"
                     Margin="0,0,15,0"
                     TextChanged="TxtBuscarProducto_TextChanged"/>

            <TextBlock Grid.Column="2" 
                       Text="Tipo:" 
                       VerticalAlignment="Center"
                       Margin="0,0,10,0"
                       FontWeight="Bold"/>

            <ComboBox Grid.Column="3"
                      x:Name="CmbTipoMovimiento"
                      Height="30"
                      Margin="0,0,15,0"
                      SelectionChanged="CmbTipoMovimiento_SelectionChanged">
                <ComboBoxItem Content="Todos" IsSelected="True"/>
                <ComboBoxItem Content="Compra"/>
                <ComboBoxItem Content="Venta"/>
                <ComboBoxItem Content="Ajuste +"/>
                <ComboBoxItem Content="Ajuste -"/>
                <ComboBoxItem Content="Devolución"/>
            </ComboBox>

            <TextBlock Grid.Column="4" 
                       Text="Desde:" 
                       VerticalAlignment="Center"
                       Margin="0,0,10,0"
                       FontWeight="Bold"/>

            <DatePicker Grid.Column="5"
                        x:Name="DpFechaDesde"
                        Height="30"
                        SelectedDateChanged="DpFecha_SelectedDateChanged"/>
        </Grid>

        <!-- DataGrid -->
        <DataGrid Grid.Row="2" 
                  x:Name="DgMovimientos"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  BorderBrush="{StaticResource PrimaryBlue}"
                  BorderThickness="1"
                  GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Fecha" 
                                    Binding="{Binding Date, StringFormat=dd/MM/yyyy HH:mm}" 
                                    Width="130"/>

                <DataGridTextColumn Header="Producto" 
                                    Binding="{Binding ProductName}" 
                                    Width="*"/>

                <DataGridTextColumn Header="Tipo" 
                                    Binding="{Binding MovementType}" 
                                    Width="100"/>

                <DataGridTextColumn Header="Cantidad" 
                                    Binding="{Binding Quantity}" 
                                    Width="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextAlignment" Value="Right"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Costo Unit." 
                                    Binding="{Binding Cost, StringFormat=C}" 
                                    Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextAlignment" Value="Right"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Proveedor" 
                                    Binding="{Binding SupplierName}" 
                                    Width="150"/>

                <DataGridTextColumn Header="Motivo" 
                                    Binding="{Binding Reason}" 
                                    Width="200"/>

                <DataGridTextColumn Header="Usuario" 
                                    Binding="{Binding User}" 
                                    Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Resumen y botones -->
        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <TextBlock Text="Total movimientos: " FontWeight="Bold" VerticalAlignment="Center"/>
                <TextBlock x:Name="TxtTotalMovimientos" Text="0" VerticalAlignment="Center" Margin="5,0"/>
            </StackPanel>

            <Button Grid.Column="1"
                    Content="Cerrar"
                    Width="80"
                    Height="30"
                    Click="BtnCerrar_Click"
                    Background="#E0E0E0"
                    Foreground="#333333"/>
        </Grid>
    </Grid>
</Window>