<Window x:Class="FastFoodApp.Client.ComboSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Personalizar Combo" 
        Height="500" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#FFF5E6">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Grid.Row="0" 
                Background="#FF6B35"
                CornerRadius="12"
                Padding="20,15"
                Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="🎁" 
                           FontSize="32"
                           VerticalAlignment="Center"
                           Margin="0,0,15,0"/>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock x:Name="TxtNombreCombo"
                               Text="Combo Clásico" 
                               FontSize="20" 
                               FontWeight="Bold" 
                               Foreground="White"/>
                    <TextBlock x:Name="TxtDescripcionCombo"
                               Text="Hamburguesa + Papas + Bebida" 
                               FontSize="12" 
                               Foreground="#FFE5E5"
                               Margin="0,3,0,0"/>
                </StackPanel>

                <Border Grid.Column="2"
                        Background="White"
                        CornerRadius="10"
                        Padding="15,8">
                    <TextBlock x:Name="TxtPrecioCombo"
                               Text="$7,500" 
                               FontSize="20" 
                               FontWeight="Black" 
                               Foreground="#FF6B35"/>
                </Border>
            </Grid>
        </Border>

        <!-- INSTRUCCIONES -->
        <TextBlock Grid.Row="1"
                   Text="✨ Personaliza tu combo seleccionando las opciones que prefieras:"
                   FontSize="13"
                   Foreground="#666"
                   Margin="0,0,0,15"/>

        <!-- LISTA DE ITEMS -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ComboItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White"
                                BorderBrush="#DDD"
                                BorderThickness="2"
                                CornerRadius="12"
                                Padding="20"
                                Margin="0,0,0,15">
                            <StackPanel>
                                <!-- NOMBRE DEL ITEM -->
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                    <TextBlock Text="{Binding Icon}" 
                                               FontSize="24"
                                               Margin="0,0,10,0"/>
                                    <TextBlock Text="{Binding GroupName}" 
                                               FontSize="16" 
                                               FontWeight="Bold" 
                                               Foreground="#333"
                                               VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding QuantityText}" 
                                               FontSize="12" 
                                               Foreground="#999"
                                               VerticalAlignment="Center"
                                               Margin="10,0,0,0"/>
                                </StackPanel>

                                <!-- OPCIONES (si permite substitución) -->
                                <ItemsControl ItemsSource="{Binding Options}" 
                                              Visibility="{Binding HasOptions}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <RadioButton GroupName="{Binding GroupName}"
                                                        Content="{Binding ProductName}"
                                                        Tag="{Binding ProductId}"
                                                        IsChecked="{Binding IsSelected}"
                                                        Margin="35,5,0,5"
                                                        FontSize="13"
                                                        Checked="RadioButton_Checked"/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <!-- PRODUCTO FIJO (sin opciones) -->
                                <TextBlock Text="{Binding DefaultProductName}" 
                                          FontSize="13" 
                                          Foreground="#666"
                                          Margin="35,0,0,0"
                                          Visibility="{Binding IsFixed}"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- BOTONES -->
        <Grid Grid.Row="3" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" 
                    Content="✓ Agregar al Pedido" 
                    Click="BtnAceptar_Click"
                    Height="50"
                    Margin="0,0,10,0"
                    FontSize="15"
                    FontWeight="Bold"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background">
                            <Setter.Value>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Color="#66BB6A" Offset="0"/>
                                    <GradientStop Color="#4CAF50" Offset="1"/>
                                </LinearGradientBrush>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="10">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#81C784" Offset="0"/>
                                            <GradientStop Color="#66BB6A" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <Button Grid.Column="1" 
                    Content="✗ Cancelar" 
                    Click="BtnCancelar_Click"
                    Height="50"
                    Margin="10,0,0,0"
                    FontSize="15"
                    FontWeight="Bold"
                    Background="White"
                    Foreground="#E74C3C"
                    BorderBrush="#E74C3C"
                    BorderThickness="2"
                    Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Grid>
</Window>